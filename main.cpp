#include "Disasm16.hpp"
#include "Executable16.hpp"

#include <iostream>

int main()
{

	Exec16 bin("test.bin");
	//printf("%d\n", (char)(0xfc));
	//for(auto instruction : bin)
	//	cpu.execute(instruction);
	
	return 0;
}
